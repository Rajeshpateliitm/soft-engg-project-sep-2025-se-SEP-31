# ============================================================================
# WasteWise Backend - Python Dependencies
# ============================================================================
# This file contains all necessary Python packages required to run the
# WasteWise backend application. Install using: pip install -r requirements.txt
#
# IMPORTANT: This file is for BACKEND (Python) dependencies only.
# For FRONTEND (JavaScript) dependencies, see: ../frontend/package.json
#
# Frontend Dependencies (installed via npm):
# - Vue.js, Vite, Pinia, Vue Router
# - Chart.js, Leaflet, Bootstrap
# - Axios, date-fns, and other JavaScript libraries
# - See ../frontend/package.json for complete list
# ============================================================================

# Core Flask Framework
# Flask: Lightweight web framework for building REST APIs
Flask==3.0.0
# Werkzeug: WSGI utility library (dependency of Flask, used for password hashing)
Werkzeug==3.0.1

# Database & ORM
# Flask-SQLAlchemy: Flask extension for SQLAlchemy ORM
Flask-SQLAlchemy==3.1.1
# SQLAlchemy: Python SQL toolkit and ORM
SQLAlchemy==2.0.44

# API & CORS
# Flask-CORS: Cross-Origin Resource Sharing (CORS) support for Flask
Flask-CORS==4.0.0

# Authentication & Security
# python-jose: JWT token implementation for authentication
python-jose[cryptography]==3.3.0
# cryptography: Cryptographic library (dependency of python-jose)
cryptography>=41.0.0

# Configuration Management
# python-dotenv: Load environment variables from .env file
python-dotenv==1.0.0

# Data Validation
# pydantic: Data validation library (used in app/schemas/token.py)
# Note: Using Pydantic v1 to match existing schema syntax (orm_mode = True)
pydantic==1.10.13
# email-validator: Email validation (dependency of pydantic EmailStr)
email-validator==1.3.1

# Email
# Flask-Mail: Email sending support for Flask
Flask-Mail==0.9.1

# ============================================================================
# FRONTEND DEPENDENCIES (JavaScript/Node.js)
# ============================================================================
# NOTE: The following are JavaScript packages and CANNOT be installed via pip.
# They must be installed using npm in the frontend directory.
# See: ../frontend/package.json for the complete list.
#
# To install frontend dependencies, run:
#   cd ../frontend
#   npm install
#
# Frontend Dependencies List (from PROJECT_REPORT.md):
# ============================================================================
# Core Framework:
#   - vue: ^3.5.22
#   - vite: ^7.1.7
#
# State Management:
#   - pinia: ^3.0.3
#   - @pinia-plugin-persistedstate/nuxt: ^1.2.1
#
# Routing:
#   - vue-router: ^4.6.3
#
# HTTP Client:
#   - axios: ^1.13.1
#
# UI Framework:
#   - bootstrap: ^5.3.8
#   - @popperjs/core: ^2.11.8
#   - tailwindcss: ^4.1.16
#
# Data Visualization:
#   - chart.js: ^4.4.0
#   - chartjs-adapter-date-fns: ^3.0.0
#   - vue-chartjs: ^5.3.0
#   - date-fns: ^4.1.0
#
# Maps & Geolocation:
#   - leaflet: ^1.9.4
#
# Development Tools:
#   - @vitejs/plugin-vue: ^6.0.1
#   - autoprefixer: ^10.4.21
#   - postcss: ^8.5.6
#
# For complete frontend dependency list, see: ../frontend/package.json
# ============================================================================

# ============================================================================
# Standard Library Modules (no installation required)
# ============================================================================
# The following modules are part of Python's standard library and do not
# need to be installed:
# - datetime (date, datetime, timedelta)
# - calendar (monthrange)
# - functools (wraps)
# - pathlib (Path)
# - os (environment variables)
# - typing (Optional, etc.)
# - sqlalchemy.exc (IntegrityError)
# ============================================================================
